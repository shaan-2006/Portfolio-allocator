This is a simple portfolio allocator which takes in the OHLC data of some instruments for a fixed period of time and computes on the data fitting ensemble models such as Randomforest and XGBoost and takes in the 
timeseries indication from ARIMA and allocates a portfolio with predicted Closing of the desired day of the week. 
